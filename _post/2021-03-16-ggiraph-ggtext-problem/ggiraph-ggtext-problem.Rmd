---
title: "font issue; mobile"
description: |
  A short description of the post.
author:
  - name: Unknown
date: 03-16-2021
output:
  distill::distill_article:
    self_contained: false
    toc: true
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = T)
plot_bg_color <- "white"

```


```{r}
library(tidyverse)
library(ggtext)
library(ggiraph)
library(extrafont)
extrafont::loadfonts(quiet=T)
library(hrbrthemes)


```

# error: ggtex + ggiraph + hrbrthemes

```{r}
my_plot <- mtcars %>% 
  ggplot()+
  labs(title=glue::glue("this is a very long title, with many lines and a line <br>break etc etc etc, this <br>word is in <span style='color:red;'>red</span> or something else etc etc"))+
  geom_point_interactive(aes(x=wt,
                             y=qsec,
                             tooltip=paste(wt, qsec)))+
  theme_ipsum_rc()+
  theme(plot.title=element_markdown())

girafe(ggobj = my_plot,
       fonts=list(sans="Roboto Condensed"),
       height_svg = 5,
       options = list(
         opts_toolbar(saveaspng = FALSE))
)

```

# ggtext + ggiraph + theme_minimal

```{r}
my_plot <- mtcars %>% 
  ggplot()+
  labs(title=glue::glue("this is a very long title, with many lines and a line <br>break etc etc etc, this <br>word is in <span style='color:red;'>red</span> or something else etc etc"))+
  geom_point_interactive(aes(x=wt,
                             y=qsec,
                             tooltip=paste(wt, qsec)))+
  theme_minimal()+
  theme(plot.title=element_markdown())

girafe(ggobj = my_plot,
       fonts=list(sans="Roboto Condensed"),
       height_svg = 5,
       options = list(
         opts_toolbar(saveaspng = FALSE))
)

```

# ggiraph + hrbrthemes

```{r}
my_plot <- mtcars %>% 
  ggplot()+
  labs(title="this is a very long title, with many lines and a line \nbreak etc etc etc, this word is in  somethingelse etc etc")+
  geom_point_interactive(aes(x=wt,
                             y=qsec,
                             tooltip=paste(wt, qsec)))+
  theme_ipsum_rc()#+
  #theme(plot.title=element_markdown())

girafe(ggobj = my_plot,
       fonts=list(sans="Roboto Condensed"),
       height_svg = 5,
       options = list(
         opts_toolbar(saveaspng = FALSE))
)
```

# ggiraph alone

```{r}
my_plot <- mtcars %>% 
  ggplot()+
  labs(title="this is a very long title, with many lines and a line \nbreak etc etc etc, this word is in  somethingelse etc etc")+
  geom_point_interactive(aes(x=wt,
                             y=qsec,
                             tooltip=paste(wt, qsec)))+
  theme_minimal()
  # theme_ipsum_rc()+
  # theme(plot.title=element_markdown())

girafe(ggobj = my_plot,
       fonts=list(sans="Roboto Condensed"),
       height_svg = 5,
       options = list(
         opts_toolbar(saveaspng = FALSE))
)
```

# only hrbrthemes

```{r}
my_plot <- mtcars %>% 
  ggplot()+
  labs(title="this is a very long title, with many lines and a line \nbreak etc etc etc, this word is in  somethingelse etc etc")+
  geom_point(aes(x=wt,
                 y=qsec))+
  theme_ipsum_rc()#+
  # theme(plot.title=element_markdown())

# girafe(ggobj = my_plot,
#        fonts=list(sans="Roboto Condensed"),
#        height_svg = 5,
#        options = list(
#          opts_toolbar(saveaspng = FALSE))
# )

my_plot
```

